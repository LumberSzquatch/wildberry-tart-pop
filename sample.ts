/**
 * This is a block comment
 */

export enum SessionGetters {
  CURRENTUSER = "SessionModule/currentUser",
  AUTHENTICATED = "SessionModule/authenticated",
  GROUPS = "SessionModule/groups",
  ISSITEADMIN = "SessionModule/isSiteAdmin"
}

// line comment
async function sample(param: string, marap?: boolean) {
  const foo = "bar";
  let poop: Boolean = false;
  SessionGetters.AUTHENTICATED;
  Sess;
}
